This is an experiment (and a neat little OTP app).

I wanted to see if I could package an app and installer in a single file, relying only on bash.

If this works the way I want, you should be able to run `. <(wget https://fordi.github.io/otp/install.sh -qO- | bash)` to install.
